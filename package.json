{
	"name": "find-ochanomizu-2022",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"wp-env": "wp-env",
		"export:db": "mkdir -p .export/sql && npm run wp-env run cli wp 'db export .export/sql/local.sql'",
		"import:db": "npm run wp-env run cli wp 'db import .export/sql/local.sql'",
		"format": "wp-scripts format",
		"lint:js": "wp-scripts lint-js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@wordpress/env": "^4.9.0",
		"@wordpress/scripts": "^23.4.0"
	},
	"dependencies": {
		"jquery-inview": "^1.1.2"
	}
}
